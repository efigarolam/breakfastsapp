import dateHelper from '../../../utils/date-helper';
import { module, test } from 'qunit';

module('dateHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = dateHelper();
  assert.ok(result);
});
